info:
  name: Create
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:3000/actor
  headers:
    - name: Authorization
      value: Bearer token
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "name": "Yuriiiii"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
