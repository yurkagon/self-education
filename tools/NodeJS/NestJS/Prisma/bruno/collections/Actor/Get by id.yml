info:
  name: Get by id
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:3000/actor/:id
  headers:
    - name: Authorization
      value: Bearer token
  params:
    - name: id
      value: a1e55000-0000-0000-0000-000000000002
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
