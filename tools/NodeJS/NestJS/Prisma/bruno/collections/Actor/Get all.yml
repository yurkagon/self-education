info:
  name: Get all
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:3000/actor
  headers:
    - name: Authorization
      value: Bearer token
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
