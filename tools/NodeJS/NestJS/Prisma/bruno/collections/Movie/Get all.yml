info:
  name: Get all
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:3000/movie
  headers:
    - name: Authorization
      value: Bearer token
  auth: inherit
  params:
    - name: search
      value: ""
      type: query
      disabled: true

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
