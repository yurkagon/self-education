info:
  name: GetAll
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/user"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  **GET /user** — list users (authenticated). Sends `accessToken` cookie after **Login** or **Register** (same Bruno cookie jar).
